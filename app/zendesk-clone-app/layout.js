import "../clones/kit/kit.css";
export const metadata = { title: "Zendisk — support desk", description: "Web widget tickets, agent replies, macros, help centre search and SLA timers." };
export default function L({ children }) { return children; }
