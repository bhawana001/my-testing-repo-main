import "../clones/kit/kit.css";
export const metadata = { title: "Mondee — the work OS", description: "Boards with status and person columns, automation recipes that fire on a status change, dashboard widgets over board data, and per-board guest sharing." };
export default function L({ children }) { return children; }
