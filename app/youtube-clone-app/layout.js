import "../clones/kit/kit.css";
export const metadata = { title: "Yootube — broadcast yourself", description: "Upload a video with a visibility setting, comment and pin as the creator, check the premium background-play entitlement, and join a channel membership tier." };
export default function L({ children }) { return children; }
