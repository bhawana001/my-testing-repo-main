import "../clones/kit/kit.css";
export const metadata = { title: "Notiond — one workspace", description: "Pages built from blocks, databases you can filter and sort, web publishing, template duplication and block comments with mentions." };
export default function L({ children }) { return children; }
