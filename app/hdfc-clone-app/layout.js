import "../clones/kit/kit.css";
export const metadata = { title: "Hindfirst Bank — NetBanking", description: "Log in with a customer id and OTP, view balances, add a beneficiary and send IMPS, open a fixed deposit, and read your card statement." };
export default function L({ children }) { return children; }
