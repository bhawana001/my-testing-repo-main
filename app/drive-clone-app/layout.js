import "../clones/kit/kit.css";
export const metadata = { title: "Drivve — files and docs", description: "Share docs at viewer, commenter or editor level, co-edit from two sessions with cursors, search by keyword and file type, and edit offline then sync." };
export default function L({ children }) { return children; }
