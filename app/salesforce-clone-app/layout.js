import "../clones/kit/kit.css";
export const metadata = { title: "Salesfource — CRM", description: "Lead capture, opportunity pipeline, reports, record editing and screen flows." };
export default function L({ children }) { return children; }
