import "../clones/kit/kit.css";
export const metadata = { title: "PhonePey — UPI payments", description: "QR scan pay, autopay mandates, filtered history and insurance quotes." };
export default function L({ children }) { return children; }
