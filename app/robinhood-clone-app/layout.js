import "../clones/kit/kit.css";
export const metadata = { title: "Robinhud — investing", description: "Market and limit orders, portfolio value, instant deposits and options chains." };
export default function L({ children }) { return children; }
