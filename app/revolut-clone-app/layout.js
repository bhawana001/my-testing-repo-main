import "../clones/kit/kit.css";
export const metadata = { title: "Revolat — everyday banking", description: "Freeze cards, exchange currency pockets, split bills and round up into vaults." };
export default function L({ children }) { return children; }
