import "../clones/kit/kit.css";
export const metadata = {
  title: "Nyke — sportswear and launches",
  description: "Size guide, member-exclusive products, launch draw entries, and saved-card checkout.",
};
export default function NikeCloneLayout({ children }) { return children; }
