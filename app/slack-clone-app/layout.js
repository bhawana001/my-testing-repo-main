import "../clones/kit/kit.css";
export const metadata = { title: "Slaick — where work happens", description: "Channels with threaded replies, file uploads with inline preview, cross-channel search, huddles and workflow forms." };
export default function L({ children }) { return children; }
