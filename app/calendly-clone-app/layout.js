import "../clones/kit/kit.css";
export const metadata = { title: "Calendari — scheduling without the back and forth", description: "Book a slot in your own timezone, see busy times excluded, reschedule from the confirmation link, and pay for a paid event." };
export default function L({ children }) { return children; }
