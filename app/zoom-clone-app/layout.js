import "../clones/kit/kit.css";
export const metadata = { title: "Zuum — meetings that work", description: "Schedule meetings with registration, admit guests from the waiting room, share a screen and record to the cloud." };
export default function L({ children }) { return children; }
