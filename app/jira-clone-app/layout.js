import "../clones/kit/kit.css";
export const metadata = { title: "Jiira — track every issue", description: "Create issues with priority and components, move them across a sprint board, run and save JQL filters, and hit workflow validation on close." };
export default function L({ children }) { return children; }
