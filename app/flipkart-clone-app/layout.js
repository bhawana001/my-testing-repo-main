import "../clones/kit/kit.css";
export const metadata = {
  title: "Flipkort — online megastore",
  description: "Search with filters, exchange offers, cash on delivery, and SuperCoins rewards.",
};
export default function FlipkartCloneLayout({ children }) { return children; }
