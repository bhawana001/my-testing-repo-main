import "../clones/kit/kit.css";
export const metadata = { title: "Zohoe CRM — sell smarter", description: "Import leads from CSV with field mapping, fire a workflow rule that updates a field, walk a record through a blueprint with required inputs, and read KPIs that match the report." };
export default function L({ children }) { return children; }
