import "../clones/kit/kit.css";
export const metadata = { title: "Squair — payments for sellers", description: "Checkout links, invoices, web POS with tipping, and dashboard refunds." };
export default function L({ children }) { return children; }
