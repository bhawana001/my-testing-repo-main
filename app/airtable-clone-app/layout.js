import "../clones/kit/kit.css";
export const metadata = { title: "Airtabel — bases that work like apps", description: "Grid records you can create, edit and delete, a shareable form view, a kanban board with stage moves, and automations with a run log." };
export default function L({ children }) { return children; }
