import "../clones/kit/kit.css";
export const metadata = {
  title: "Stripey — payments platform",
  description: "Hosted checkout, 3DS challenges, declined-card recovery, billing portal, and a payments dashboard.",
};
export default function StripeCloneLayout({ children }) { return children; }
