import "../clones/kit/kit.css";
export const metadata = { title: "Coinbaze — buy and sell crypto", description: "Card purchases, recurring buys, address validation and price alerts." };
export default function L({ children }) { return children; }
