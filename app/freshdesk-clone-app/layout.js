import "../clones/kit/kit.css";
export const metadata = { title: "Freshdesc — support made simple", description: "Email that becomes a ticket, canned responses with placeholders resolved, ticket merging that keeps both threads, and a customer portal view." };
export default function L({ children }) { return children; }
