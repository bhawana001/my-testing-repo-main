import "../clones/kit/kit.css";
export const metadata = { title: "Zerodhaa Kyte — trading", description: "Intraday orders, GTT triggers, holdings P&L and UPI fund transfers." };
export default function L({ children }) { return children; }
