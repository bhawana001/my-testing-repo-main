import "../clones/kit/kit.css";
export const metadata = { title: "Hotstarr — live sport and shows", description: "Live match streams with a score overlay and live indicator, plus a catalogue of shows and movies." };
export default function L({ children }) { return children; }
