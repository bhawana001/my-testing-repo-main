import "../clones/kit/kit.css";
export const metadata = { title: "Dropbaks — your files, everywhere", description: "Upload files, create view-only share links, change folder permissions, collect files by request and restore an earlier version." };
export default function L({ children }) { return children; }
