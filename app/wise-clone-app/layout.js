import "../clones/kit/kit.css";
export const metadata = { title: "Wize — international money transfers", description: "Transfer quotes with fee breakdowns, recipients, transfer tracking and balance conversion." };
export default function L({ children }) { return children; }
