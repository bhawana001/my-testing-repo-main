import "../clones/kit/kit.css";
export const metadata = { title: "PayPaal — pay and get paid", description: "Express checkout, guest card payments, P2P transfers, disputes and currency conversion." };
export default function L({ children }) { return children; }
