import "../clones/kit/kit.css";
export const metadata = { title: "ServiceNau — the platform of platforms", description: "Raise incidents with a derived priority, push a change request through approval, order from the service catalog, and search the knowledge base." };
export default function L({ children }) { return children; }
