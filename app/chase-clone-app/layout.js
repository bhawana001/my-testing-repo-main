import "../clones/kit/kit.css";
export const metadata = { title: "Chaise Bank — online banking", description: "2FA login, Zelly transfers, bill pay scheduling, statements and card disputes." };
export default function L({ children }) { return children; }
