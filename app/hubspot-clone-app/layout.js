import "../clones/kit/kit.css";
export const metadata = { title: "Hubsprout — grow better", description: "Landing page forms that create contacts, a deal pipeline that preserves amounts across stages, email sequence enrolment on the contact timeline, and a bookable meeting link." };
export default function L({ children }) { return children; }
