import "../clones/kit/kit.css";
export const metadata = { title: "Teemz — chat, meet, collaborate", description: "Join meetings from the calendar, post with @ mentions that land in Activity, co-author shared docs and load custom tab apps." };
export default function L({ children }) { return children; }
