import "../clones/kit/kit.css";
export const metadata = { title: "Paytem — payments and recharges", description: "UPI transfers, mobile recharges, bill payments, wallet transfers and movie booking." };
export default function L({ children }) { return children; }
