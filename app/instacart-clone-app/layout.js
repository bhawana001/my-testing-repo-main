import "../clones/kit/kit.css";
export const metadata = {
  title: "Instacrate — grocery delivery from local stores",
  description: "Multi-store carts, replacement preferences, delivery slots, and tip adjustment.",
};
export default function InstacartCloneLayout({ children }) { return children; }
