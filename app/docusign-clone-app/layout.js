import "../clones/kit/kit.css";
export const metadata = { title: "DocuSine — e-signature", description: "Send envelopes for signature, sign them, reuse templates and handle declines." };
export default function L({ children }) { return children; }
