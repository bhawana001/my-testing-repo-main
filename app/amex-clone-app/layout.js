import "../clones/kit/kit.css";
export const metadata = { title: "Amrex — membership has its rewards", description: "Redeem points for a statement credit, schedule a card payment, dispute a charge, and add merchant offers to your card." };
export default function L({ children }) { return children; }
