import "../clones/kit/kit.css";
export const metadata = { title: "Asanah — work, organised", description: "Tasks with assignees and due dates, a board with section history, subtasks and dependencies that block completion, and My Tasks grouped by due date." };
export default function L({ children }) { return children; }
