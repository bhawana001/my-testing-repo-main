import "../clones/kit/kit.css";
export const metadata = { title: "Klarnah — pay in instalments", description: "Pay in 4, credit decisions, payment schedules and returns that recalculate instalments." };
export default function L({ children }) { return children; }
