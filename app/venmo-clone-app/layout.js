import "../clones/kit/kit.css";
export const metadata = { title: "Venmoo — pay friends", description: "P2P payments with notes and privacy, requests with reminders, cash out and a card feed." };
export default function L({ children }) { return children; }
