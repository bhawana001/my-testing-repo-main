import "../clones/kit/kit.css";
export const metadata = { title: "Intercomm — customer messaging", description: "Start a messenger conversation with an attachment, hand off from bot to a human with context, get article suggestions as you type, and see targeted outbound messages." };
export default function L({ children }) { return children; }
