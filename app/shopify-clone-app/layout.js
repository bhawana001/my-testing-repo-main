import "../clones/kit/kit.css";

export const metadata = {
  title: "Shoplify — merchant storefront and admin",
  description: "Storefront checkout, discounts, checkout extensions, and admin order management.",
};

export default function ShopifyCloneLayout({ children }) {
  return children;
}
