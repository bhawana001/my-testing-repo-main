import "../clones/kit/kit.css";
export const metadata = { title: "Razorpie — payment gateway", description: "Checkout modal, UPI intent, EMI plans and payment links." };
export default function L({ children }) { return children; }
