import "../clones/kit/kit.css";
export const metadata = { title: "Adyeen — unified payments", description: "Drop-in with 3DS2, iDEAL redirects, stored card tokens and webhook status parity." };
export default function L({ children }) { return children; }
